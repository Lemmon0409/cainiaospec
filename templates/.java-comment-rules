# Java Code Comment Rules

## 强制规则：编写 Java 代码时必须添加 JavaDoc 注释

---

## Controller 类

```java
/**
 * [业务功能概述]
 *
 * <p>服务场景：[哪些用户/系统使用]
 * <p>核心功能：[功能1]、[功能2]、[功能3]
 */
@RestController
public class XxxController {

    /**
     * [接口用途]
     *
     * <p>业务逻辑：
     * 1. [步骤1]
     * 2. 调用 {@link XxxService#method}
     * 3. [步骤3]
     *
     * @param req [参数含义]
     * @return [返回值含义]
     */
    @PostMapping("/xxx")
    public Result<T> method(Req req) {}
}
```

---

## Service 类

```java
/**
 * [服务职责概述]
 *
 * <p>依赖：{@link XxxRepository}、{@link YyyService}
 */
@Service
public class XxxServiceImpl {

    /**
     * [方法业务目的]
     *
     * <p>执行步骤：
     * 1. [步骤1]
     * 2. [步骤2]
     * 3. [步骤3]
     *
     * <p>业务规则：
     * - [规则1]
     * - [规则2]
     *
     * @param xxx [参数含义]
     * @return [返回值含义]
     * @throws XxxException [触发条件]
     */
    public T method(P param) {}
}
```

---

## DTO/Entity 类

```java
/**
 * [类用途 - 如"订单创建请求"]
 *
 * <p>使用场景：[哪个接口使用]
 */
@Data
public class XxxReq {

    /**
     * [字段业务含义]
     * <p>示例：[示例值]
     * <p>单位：[如"秒"、"分"]（如适用）
     * <p>取值：[枚举值列表]（如适用）
     */
    private String field;
}
```

---

## 枚举类

```java
/**
 * [枚举用途]
 */
public enum XxxEnum {
    
    /** [枚举值含义] - [可执行操作] */
    VALUE1("code", "name"),
    
    /** [枚举值含义] - [可执行操作] */
    VALUE2("code", "name");
}
```

---

## 注释要求

### ✅ 必须

- 用**业务语言**描述，不要只翻译变量名
- Controller 方法说明**调用链**（调用了哪个 Service）
- 复杂方法列出**执行步骤**（1. 2. 3.）
- 重要方法标注**业务规则**
- 字段注释给出**示例值**
- 数值字段标注**单位**

### ❌ 禁止

- 无意义注释：`/** 获取ID */ getId()`
- 空注释：`/** */`
- 只写英文（核心业务用中文）

---

## 简化场景

以下可简化为一句话注释：
- 工具类方法
- 私有方法
- Getter/Setter（用 Lombok 省略）
